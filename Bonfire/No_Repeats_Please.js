function permAlone(str) {
    // split the variable "str" into an array
    
    // create a multiple loops that test taking the last variable and moving it to position 0 -> (n - 1)
    // test each of these possibilites for "no repeats" and save it if it is true else move on.
  return str;
}

permAlone('aab');